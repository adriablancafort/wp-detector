<section class="container container__headline">
  <h1 class="headline--title">
    WordPress <em>Theme</em> Detector and WordPress <em>Plugin</em> Detector
  </h1>
  <p class="headline--description">
    Just enter any website URL and WordPress Detector will tell you which
    WordPress <strong>theme</strong> and WordPress <strong>plugins</strong> it is
    using and much more!
  </p>

  <img
    class="headline--cards-image headline--cards-image__left hide-1000"
    src="/wordpress-detector-cards-left.webp"
    alt="Cards Image"
    width="360px"
    height="auto"
  />
  <img
    class="headline--cards-image headline--cards-image__right hide-1000"
    src="/wordpress-detector-cards-right.webp"
    alt="Cards Image"
    width="360px"
    height="auto"
  />
</section>

<style is:global>
  .container__headline {
    text-align: center;
  }

  .headline--title {
    margin: 0 auto;
    padding: 2rem 0;
    max-width: 900px;
    font-size: 4.5em;
    animation: fade-in-up 0.6s ease-out;
  }

  .headline--title em {
    font-family: "EB Garamond";
    position: relative;

    &::after {
      content: "";
      position: absolute;
      bottom: 0.25rem;
      left: -0.4rem;
      right: -0.8rem;
      height: 1rem;

      z-index: -1;
      background-image: url("/underline.svg");
      background-repeat: no-repeat;
      background-size: cover;
    }
  }

  .headline--description {
    margin: 0 auto;
    max-width: 700px;
    font-size: 1.2em;
    animation: fade-in-up 0.7s ease-out;
  }

  .headline--cards-image {
    position: absolute;
    top: 100px;
    z-index: -1;
  }

  .headline--cards-image__left {
    left: 0;
    animation: slide-left 0.7s ease-out;
  }

  .headline--cards-image__right {
    right: 0;
    animation: slide-right 0.7s ease-out;
  }

  @keyframes fade-in-up {
    0% {
      transform: translateY(20px);
      filter: blur(5px);
    }
    100% {
      transform: translateY(0);
      filter: blur(0);
    }
  }

  @keyframes slide-left {
    0% {
      transform: translateX(-20px);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes slide-right {
    0% {
      transform: translateX(20px);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }
</style>
